
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="./public/style.css">
    <h1>Welcome to {{userInfo.firstName}}'s Profile!</h1>
</head>
<body>
    <div class = "myDiv"><h1>Username: {{userInfo.username}}</h1></div>
    <div>
        {{#if userInfo.isNotMe}} 
            <a href="" class="followLink">{{userInfo.isFollowing}}</a>
        {{/if}}
    </div>
    <div>Name: {{userInfo.firstName}} {{userInfo.lastName}}</div>
    {{#if userInfo.favoriteCocktail}}
    <div>Favorite Cocktail: {{userInfo.favoriteCocktail}}</div>
    {{/if}}
    <div>Birthday: {{userInfo.dateOfBirth}}</div>
    <div>Membership Age: {{userInfo.daysMembership}} days</div>
    <div>Uploaded Posts:</div>
    <div id="postColumn">    
        {{#each userInfo.posts}} {{> post}} {{/each}}
    </div>
    <div>Uploaded Recipes:</div>
    <div>
        {{#each userInfo.recipes}} 
            <a href="/recipes/{{this.[0]}}">
                <p>{{ this.[1] }}</p>
            </a>
        {{/each}}
    </div>

    <p class="userID" hidden>
        {{userInfo._id}}
    </p>

    
    <footer><a href="/edit/profile/{{userInfo._id}}">Edit</a>{{!-- <button type="submit" id = "editProfile" class="btn btn-default" action = "/edit/profile/{{userInfo._id}}" value = "Edit Profile">Edit Profile</button> --}}
        <div>HTML: How to Mix Liquor, Created Spring 2021</div></footer>

        <script src="/public/js/profile.js"></script>
    
</body>
</html>